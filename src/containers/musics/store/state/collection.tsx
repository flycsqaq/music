import { MusicCollectionState, MenuCollectonState } from "../../models/state";
import { recentlyListState } from "./recently";

export const musicCollectionState: MusicCollectionState = {
  recently: recentlyListState
}

export const menuCollectionState: MenuCollectonState = {}